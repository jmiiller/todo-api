type Query {
  todoItems: [TodoItem]!
}

type Mutation {
  putTodoItem(input: PutTodoItemInput!): TodoItem!
  deleteTodoItem(id: String!): TodoItem!
}

input PutTodoItemInput {
  content: String!
}

type TodoItem {
  id: String!
  content: String!
}